go to releases to download the game, use the corresponding zip file for the operating system you are using
when unzipped activate the executable file your system uses that contains the game, this will automatically create a desktop shortcut
before opening somee permission work may have to be done so your machine does not recognise it as malware

on mac open a terminal, set the file directory [cd ~/Downloads/The\ Outsiders]
then [chmod +x]
if chmod doesnt work try [xattr -d com.apple.quarantine]

on windows try pressing run anway when prompted on opening
